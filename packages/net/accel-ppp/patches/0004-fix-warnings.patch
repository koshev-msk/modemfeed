diff --git a/accel-pppd/cli/std_cmd.c b/accel-pppd/cli/std_cmd.c
index c37777b..57bd475 100644
--- a/accel-pppd/cli/std_cmd.c
+++ b/accel-pppd/cli/std_cmd.c
@@ -58,8 +58,8 @@ static int show_stat_exec(const char *cmd, char * const *fields, int fields_cnt,
 	cli_sendv(client, "mem(rss/virt): %lu/%lu kB\r\n", vmrss * page_size_kb, vmsize * page_size_kb);
 #endif
 	cli_send(client, "core:\r\n");
-	cli_sendv(client, "  mempool_allocated: %" PRIu64 "\r\n", triton_stat.mempool_allocated);
-	cli_sendv(client, "  mempool_available: %" PRIu64 "\r\n", triton_stat.mempool_available);
+	cli_sendv(client, "  mempool_allocated: %lu\r\n", triton_stat.mempool_allocated);
+	cli_sendv(client, "  mempool_available: %lu\r\n", triton_stat.mempool_available);
 	cli_sendv(client, "  thread_count: %u\r\n", triton_stat.thread_count);
 	cli_sendv(client, "  thread_active: %u\r\n", triton_stat.thread_active);
 	cli_sendv(client, "  context_count: %u\r\n", triton_stat.context_count);
diff --git a/accel-pppd/ctrl/ipoe/ipoe.c b/accel-pppd/ctrl/ipoe/ipoe.c
index 6ce35a6..d0aa993 100644
--- a/accel-pppd/ctrl/ipoe/ipoe.c
+++ b/accel-pppd/ctrl/ipoe/ipoe.c
@@ -219,8 +219,10 @@ static void ipoe_session_keepalive(struct dhcpv4_packet *pack);
 static void add_interface(const char *ifname, int ifindex, const char *opt, int parent_ifindex, int vid, int vlan_mon);
 static int get_offer_delay();
 static void __ipoe_session_start(struct ipoe_session *ses);
+#ifdef RADIUS
 static int ipoe_rad_send_auth_request(struct rad_plugin_t *rad, struct rad_packet_t *pack);
 static int ipoe_rad_send_acct_request(struct rad_plugin_t *rad, struct rad_packet_t *pack);
+#endif
 static void ipoe_session_create_auto(struct ipoe_serv *serv);
 static void ipoe_serv_timeout(struct triton_timer_t *t);
 static struct ipoe_session *ipoe_session_create_up(struct ipoe_serv *serv, struct ethhdr *eth, struct iphdr *iph, struct _arphdr *arph);
@@ -582,10 +584,6 @@ static void ipoe_change_l4_redirect(struct ipoe_session *ses, int del)
 		triton_timer_del(&ses->l4_redirect_timer);
 }
 
-static void ipoe_change_addr(struct ipoe_session *ses, in_addr_t newaddr)
-{
-
-}
 
 static int ipoe_create_interface(struct ipoe_session *ses)
 {
diff --git a/accel-pppd/ctrl/l2tp/l2tp.c b/accel-pppd/ctrl/l2tp/l2tp.c
index d680737..2ae414c 100644
--- a/accel-pppd/ctrl/l2tp/l2tp.c
+++ b/accel-pppd/ctrl/l2tp/l2tp.c
@@ -1957,11 +1957,15 @@ static int l2tp_session_connect(struct l2tp_sess_t *sess)
 	pppox_addr.pppol2tp.s_session = sess->sid;
 	pppox_addr.pppol2tp.d_session = sess->peer_sid;
 
-	if (connect(sess->ppp.fd,
-		    (struct sockaddr *)&pppox_addr, sizeof(pppox_addr)) < 0) {
-		log_session(log_error, sess, "impossible to connect session:"
+	{
+		struct sockaddr_storage _sa;
+		memcpy(&_sa, &pppox_addr, sizeof(pppox_addr));
+		if (connect(sess->ppp.fd,
+		    (struct sockaddr *)&_sa, sizeof(pppox_addr)) < 0) {
+			log_session(log_error, sess, "impossible to connect session:"
 			    " connect() failed: %s\n", strerror(errno));
-		goto out_err;
+			goto out_err;
+		}
 	}
 
 	if (setsockopt(sess->ppp.fd, SOL_PPPOL2TP, PPPOL2TP_SO_LNSMODE,
@@ -2073,11 +2077,15 @@ static int l2tp_tunnel_connect(struct l2tp_conn_t *conn)
 		goto err_fd;
 	}
 
-	if (connect(tunnel_fd,
-		    (struct sockaddr *)&pppox_addr, sizeof(pppox_addr)) < 0) {
-		log_tunnel(log_error, conn, "impossible to connect tunnel:"
+	{
+		struct sockaddr_storage _sa;
+		memcpy(&_sa, &pppox_addr, sizeof(pppox_addr));
+		if (connect(tunnel_fd,
+		    (struct sockaddr *)&_sa, sizeof(pppox_addr)) < 0) {
+			log_tunnel(log_error, conn, "impossible to connect tunnel:"
 			   " connect() failed: %s\n", strerror(errno));
-		goto err_fd;
+			goto err_fd;
+		}
 	}
 
 	if (conf_hello_interval)
diff --git a/accel-pppd/ctrl/pptp/pptp.c b/accel-pppd/ctrl/pptp/pptp.c
index 24984db..bd23fe4 100644
--- a/accel-pppd/ctrl/pptp/pptp.c
+++ b/accel-pppd/ctrl/pptp/pptp.c
@@ -325,18 +325,30 @@ static int pptp_out_call_rqst(struct pptp_conn_t *conn)
 
 	fcntl(pptp_sock, F_SETFD, fcntl(pptp_sock, F_GETFD) | FD_CLOEXEC);
 
-	if (bind(pptp_sock, (struct sockaddr*)&src_addr, sizeof(src_addr))) {
-		log_ppp_error("failed to bind PPTP socket (%s)\n", strerror(errno));
-		close(pptp_sock);
-		return -1;
+	{
+		struct sockaddr_storage _sa;
+		memcpy(&_sa, &src_addr, sizeof(src_addr));
+		if (bind(pptp_sock, (struct sockaddr *)&_sa, sizeof(src_addr))) {
+			log_ppp_error("failed to bind PPTP socket (%s)\n", strerror(errno));
+			close(pptp_sock);
+			return -1;
+		}
 	}
 	addrlen = sizeof(src_addr);
-	getsockname(pptp_sock, (struct sockaddr*)&src_addr, &addrlen);
+	{
+		struct sockaddr_storage _sa;
+		getsockname(pptp_sock, (struct sockaddr *)&_sa, &addrlen);
+		memcpy(&src_addr, &_sa, sizeof(src_addr));
+	}
 
-	if (connect(pptp_sock, (struct sockaddr*)&dst_addr, sizeof(dst_addr))) {
-		log_ppp_error("failed to connect PPTP socket (%s)\n", strerror(errno));
-		close(pptp_sock);
-		return -1;
+	{
+		struct sockaddr_storage _sa;
+		memcpy(&_sa, &dst_addr, sizeof(dst_addr));
+		if (connect(pptp_sock, (struct sockaddr *)&_sa, sizeof(dst_addr))) {
+			log_ppp_error("failed to connect PPTP socket (%s)\n", strerror(errno));
+			close(pptp_sock);
+			return -1;
+		}
 	}
 
 	if (send_pptp_out_call_rply(conn, msg, src_addr.sa_addr.pptp.call_id, PPTP_CALL_RES_OK, 0))
diff --git a/accel-pppd/extra/pppd_compat.c b/accel-pppd/extra/pppd_compat.c
index 12363cd..de48f06 100644
--- a/accel-pppd/extra/pppd_compat.c
+++ b/accel-pppd/extra/pppd_compat.c
@@ -166,7 +166,7 @@ static void ip_down_handler(struct sigchld_handler_t *h, int status)
 	triton_context_wakeup(pd->ses->ctrl->ctx);
 }
 
-static void ip_change_handler(struct sigchld_handler_t *h, int status)
+static void __attribute__((unused)) ip_change_handler(struct sigchld_handler_t *h, int status)
 {
 	struct pppd_compat_pd *pd = container_of(h, typeof(*pd), hnd);
 
diff --git a/accel-pppd/log.c b/accel-pppd/log.c
index e4a1e6e..d46a53d 100644
--- a/accel-pppd/log.c
+++ b/accel-pppd/log.c
@@ -78,6 +78,8 @@ static void do_log(int level, const char *fmt, va_list ap, struct ap_session *se
 
 	if (!stat_buf) {
 		stat_buf = _malloc(LOG_MAX_SIZE + 1);
+		if (!stat_buf)
+			return;
 		pthread_setspecific(stat_buf_key, stat_buf);
 	}
 
