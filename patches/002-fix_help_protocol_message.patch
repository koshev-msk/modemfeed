diff --git a/ndpi-netfilter/ipt/libxt_ndpi.c b/ndpi-netfilter/ipt/libxt_ndpi.c
index 3b302fb..8d357e0 100644
--- a/ndpi-netfilter/ipt/libxt_ndpi.c
+++ b/ndpi-netfilter/ipt/libxt_ndpi.c
@@ -306,9 +306,21 @@ static int cmp_pname(const void *p1, const void *p2) {
 	return 0;
 }
 
+static char *strupr(char *s)
+{
+	char *tmp = s;
+
+	for (; *tmp; ++tmp) {
+		*tmp = toupper((unsigned char) *tmp);
+	}
+
+	return s;
+}
+
 static int ndpi_print_prot_list(int cond) {
         int i,c,d,l;
 	char line[128];
+	char name[32];
 	char *pn[NDPI_LAST_IMPLEMENTED_PROTOCOL+1];
 
 	bzero((char *)&pn[0],sizeof(pn));
@@ -322,6 +334,12 @@ static int ndpi_print_prot_list(int cond) {
 	    pn[i-1] = prot_short_str[i];
 	}
 	qsort(&pn[0],NDPI_LAST_IMPLEMENTED_PROTOCOL-1,sizeof(pn[0]),cmp_pname);
+        for (i = 0; i <= NDPI_LAST_IMPLEMENTED_PROTOCOL; i++) {
+	    if(!pn[i]) break;
+	    strcpy(name, pn[i]);
+	    printf("--%s Match for %s protocol packets.\n", pn[i], strupr(name));
+	}
+#if 0
         for (i = 0,c = 0,l=0; i <= NDPI_LAST_IMPLEMENTED_PROTOCOL; i++) {
 	    if(!pn[i]) break;
 	    l += snprintf(&line[l],sizeof(line)-1-l,"%-20s ", pn[i]);
@@ -332,6 +350,7 @@ static int ndpi_print_prot_list(int cond) {
 	    }
 	}
 	if(c > 0) printf("%s\n",line);
+#endif
 	return d;
 }
 
